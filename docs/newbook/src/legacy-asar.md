# Legacy Asar Manual

Most pages in this manual originated from the Asar 1.x documentation. They
remain useful for core syntax, but may not describe Z3ASM-specific behavior.

If you are looking for Z3ASM features (structured outputs, z3dk.toml, hooks),
start with:
- **Z3ASM Overview** (`z3asm.md`)
- **Differences vs Asar** (`z3asm-differences.md`)
- **Asar 2.0 compatibility** (`z3asm-compat.md`)
